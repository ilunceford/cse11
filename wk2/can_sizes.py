import math






def compute_volume(name, r, h, price):
    v = math.pi * (r ** 2) * h
    return v

def main():
    volume = 0
    radius = 0 
    height = 0
    surface_area = 0
    volume = compute_volume("#1", 6.83, 10.16, "$"0.28)


def compute_surface_area(r, h):
    sa = 2 * math.pi * r * (r + h)
    return sa
    














